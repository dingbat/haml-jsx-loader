const text = (~
  .
    %textarea(name="message"
              placeholder="Your message here..."
              defaultValue={this.props.message})

    .(name="message"
      placeholder="Your message here..."
      defaultValue={this.props.message})

    .(
      name="message"
      placeholder="Your message here..."
      defaultValue={this.props.message}
      )
~)